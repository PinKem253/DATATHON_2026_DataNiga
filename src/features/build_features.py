import pandas as pd


def build_sales_features(df):
    """
    Build features for sales forecasting.
    For baseline, use simple time-based features.
    """
    # Already done in preprocess, but can add more if needed
    # For baseline, just return as is
    return df
